{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Start Server",
			"type": "shell",
			"command": "bundle config set --local path 'vendor/bundle' && bundle install && bundle exec jekyll serve --livereload --trace --port 4001",
			"presentation": {
				"reveal": "always",
				"panel": "shared"
			},
			"problemMatcher": []
		}
	]
}
